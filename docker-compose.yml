version: '3.8'

services:
  # PostgreSQL Database
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: isc_postgres
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: isc_db
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - isc_network
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # # Redis Cache
  # redis:
  #   image: redis:7-alpine
  #   container_name: isc_redis
  #   restart: unless-stopped
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   networks:
  #     - isc_network
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 10s
  #     timeout: 3s
  #     retries: 5

  # Backend API
  backend:
    build:
      context: ./be
      dockerfile: Dockerfile
    container_name: isc_backend
    restart: unless-stopped
    ports:
      - "8888:8888"
    # environment:
    #   Variables are now defined in Dockerfile
    volumes:
      - ./be/uploads:/app/uploads
      - ./be/logs:/app/logs
    # depends_on:
    #   postgres:
    #     condition: service_healthy
    #   redis:
    #     condition: service_healthy
    networks:
      - isc_network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:8888/api/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

# volumes:
#   postgres_data:
#     driver: local
#   redis_data:
#     driver: local

networks:
  isc_network:
    driver: bridge
